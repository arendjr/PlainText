{
  "name": "Joe",
  "triggers": { "oninit": "(function() { var self = this; this.setInterval(function() { var exits = self.currentArea.exits; var tries = 0; do { var exit = exits[randomInt(0, exits.length)]; if (!exit.hidden && (!exit.door || exit.open)) { self.go(exit); return; } tries++; } while (tries < 3); }, 5000); })", "onspawn": "(function() { var self = this; this.setInterval(function() { var exits = self.currentArea.exits; var tries = 0; do { var exit = exits[randomInt(0, exits.length)]; if (!exit.hidden && (!exit.door || exit.open)) { self.go(exit); return; } tries++; } while (tries < 3); }, 5000); })" },
  "plural": "Joes",
  "portable": false,
  "weight": 0,
  "currentArea": "area:85",
  "race": "race:2",
  "characterClass": "0",
  "gender": "male",
  "stats": [0, 0, 0, 0, 0, 0, 0, 0],
  "respawnTime": 8000,
  "respawnTimeVariation": 8000,
  "hp": 1,
  "maxHp": 1,
  "mp": 0,
  "maxMp": 1,
  "gold": 0
}
