{
  "name": "cat",
  "description": "The cat has a nice brown-greyish fur with black stripes over its back and head.",
  "triggers": { "oninit": "(function() { this.invokeTrigger('onspawn'); })", "onspawn": "(function() { var self = this; this.setInterval(function() { var exits = self.currentArea.exits; var tries = 0; do { var exit = exits[randomInt(0, 2 * exits.length)]; if (exit === undefined) { return; } if (!exit.hidden && (!exit.door || exit.open) && exit.name !== 'gate') { self.go(exit); return; } tries++; } while (tries < 3); }, 40000); })" },
  "plural": "cats",
  "indefiniteArticle": "a",
  "portable": false,
  "weight": 8,
  "cost": 0,
  "currentArea": "area:65",
  "race": "0",
  "characterClass": "0",
  "gender": "male",
  "stats": [3, 60, 4, 2, 0, 0, 30, 8],
  "respawnTime": 120000,
  "respawnTimeVariation": 180000,
  "hp": 1,
  "maxHp": 8,
  "mp": 0,
  "maxMp": 0,
  "gold": 0
}
